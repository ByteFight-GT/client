"use client";

import React from 'react';

type RunnerPageProps = {
	
};

const RunnerPage = (props: RunnerPageProps) => {
	return (
		<div>
			<h1>Run game here, takes to /matchplayer after</h1>
		</div>
	);
};

export default RunnerPage;
